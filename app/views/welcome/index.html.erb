<h2>Recent Posts</h2>

<div class="posts">
  <% @posts.each do |p|  %>
  <h3><%= p.title %></h3></br>
  <%= p.message %></br>
  <strong><%= p.user.username %></br></strong>
  <%= link_to("Comment", "/posts/" + p.id.to_s) %>
  <% end %>
</div>
